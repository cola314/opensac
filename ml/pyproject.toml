[project]
name = "opensac-ml"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "dspy[optuna]>=2.6",
    "anthropic>=0.52",
    "tqdm",
]

[project.optional-dependencies]
dev = [
    "pytest",
]
